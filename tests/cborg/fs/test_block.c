/**
 * Copyright (c) 2020-2021 Adil Benhlal <adil.benhlal@outlook.fr>
 * 
 */

#include <stdio.h>
#include <assert.h>
#include <unistd.h>

#include "cborg/fs/block.h"

int main() {
  // block_t *blk = cb_block_new("../test_block.blk");
  // block_t *blk2 = cb_block_new("../test_block2.blk");
  // memset(blk->data,'0',blk->blksize);
  // memset(blk2->data,'1',blk2->blksize);
  // blk->data[blk->blksize - 1] = '\n';
  // blk2->data[blk2->blksize - 1] = '\n';
  // cb_block_write(blk, 0);
  // cb_block_write(blk2, 5);
  // cb_block_delete(blk);
  // cb_block_delete(blk2);
  return EXIT_SUCCESS;
}
